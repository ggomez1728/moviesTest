# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'merqueo-ios-test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for merqueo-ios-test
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  
  target 'merqueo-ios-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'merqueo-ios-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
